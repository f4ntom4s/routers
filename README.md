To run the solution just run:

python <node file> <arcs file> <node of origin>

or include the main file in your project and import the function:
run_single_instance(nodes_file, arcs_file, origin_index)
